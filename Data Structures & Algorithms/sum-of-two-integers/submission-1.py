class Solution:
    def getSum(self, a: int, b: int) -> int:
        # there is a builtin fuction but i don't use it here:
        #if a < 0:
            #a = 0 - a
        #if b < 0:
            #b = 0 - b
        answer = a+b
        return answer